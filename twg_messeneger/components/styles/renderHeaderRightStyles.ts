import { StyleSheet } from 'react-native';

export const renderHeaderRightStyles = StyleSheet.create({
    headerRightContainer: {
        flexDirection: 'row',
        gap: 8,
        paddingRight: 16
        },
})